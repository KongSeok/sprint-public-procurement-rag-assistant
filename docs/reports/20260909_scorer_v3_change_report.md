# 채점기 v3 수정 및 v2 결과 검토
작성일: 2026-09-09

1. 검토한 v2 실행

- 실행 폴더: output/scoring_v2_runs/20260909T075822Z
- 대상: Golden Set v3 로더가 반환한 79문항
- 생성 성공: 79/79 (100%)
- 실행 오류/빈 응답: 0건/0건
- v2 end_to_end_score: 68.8505
- v2 lexical_fact_score 평균: 75.8073
- Set macro F1: 0.4519
- 인용 형식 통과율: 0%

인용 형식이 0%인 이유는 생성 답변이 주로 "근거: 파일명" 형태이고, 현재 평가
계약은 답변 마지막의 "[근거: doc_id]" 형태를 요구하기 때문이다. 이는 채점기
정규화 문제가 아니라 생성 출력 형식과 평가 계약의 불일치다.

2. v2 결과에서 실제로 확인된 기권 문제

- supplemental-qa-g22의 "확인되지 않습니다"가 answered로 분류됨
- supplemental-qa-g24의 기권 설명이 partial_answer로 분류됨

v2에 decision=abstain 분기는 있었지만 기권 문구 인식 범위가 충분하지 않았다.
v3는 두 문항을 모두 abstained로 분류하고 각각 100점으로 반영한다.

반대로 다음 3문항은 답해야 하는 문항인데 모델이 완전 기권했으므로 0점이 맞다.

- supplemental-qa-c23
- supplemental-qa-g25
- visual-hwp-figure-002

3. v3의 5번 개선: 한국어 조사/어미

- 정답과 답변을 Kiwi 형태소 분석기로 분해
- 조사(J*)와 어미(E*)는 비교에서 제외
- 명사와 동사/형용사의 형태소를 비교
- 정규식으로 "음/함/됨"을 무조건 자르지 않으므로 마음, 도움, 포함 같은 명사를
  훼손하지 않음
- 참여 가능/참여할 수 있음, 참여 불가능/참여할 수 없음은 제한적으로 동치 처리
- 참여 가능/불가능, 포함/미포함, 충족/미충족의 명백한 반대 표현은 통과 방지

4. v3의 6번 개선: 원문 표기와 재해석 표기

- "100분의 N"과 "N%"를 동치 처리
- "20억"과 "20억원"처럼 한국어 금액 단위 뒤의 원 유무를 동치 처리
- 이상/이하/초과/미만은 제거하지 않고 반드시 같은 방향인지 확인
- 원본 골든셋과 생성 답변은 수정하지 않음

5. 동일한 79개 저장 답변의 v3 읽기 전용 재채점 결과

- v3.0.1 end_to_end_score: 70.1163 (v2 대비 +1.2658%p)
- v3.0.1 lexical_fact_score 평균: 74.2448
- 검색 Recall: 0.8443 (v2 inference.jsonl에서 재집계)
- Context fact coverage: 0.6620 (v2 inference.jsonl에서 재집계)
- 기권 일치율: 0.9545
- Set macro F1: 0.4519 (채점 로직을 바꾸지 않아 동일)
- 인용 형식 통과율: 0% (생성 답변을 다시 만들지 않아 동일)

v2 당시 summary.json에는 검색 Recall과 Context fact coverage 필드가 출력되지
않았지만, 문항별 inference.jsonl에는 값이 저장되어 있었다. v3 재채점에서 이를
평균 내어 각각 0.8443, 0.6620으로 복원했다. Set Macro F1은 v2 summary.json에도
0.4519로 기록되어 있다.

이 비교는 생성 API를 다시 호출하지 않고 v2의 inference.jsonl을 그대로 사용했다.
따라서 차이는 생성 노이즈가 아니라 채점기 변경에서 발생한 것이다.

초기 v3.0.0 검증에서는 74.6732점이 나왔지만, 세부 검수 결과 숫자를 답하지 않은
c18/g23 등이 형태소 40% 일치만으로 통과하는 과대채점이 발견됐다. 일반 숫자 일치
조건을 복원하고 형태소 fallback을 100% 일치로 제한한 v3.0.1에서는 70.1163점이다.
따라서 v3 실행 폴더 20260909T084747Z의 74.6732점은 최종 성적으로 사용하지 않는다.

6. 주요 사례 해석

- c16(20억/20억원): v2와 v3 모두 100점. 현재 답변에는 필요한 의미가 충분히 있었음.
- c19: v3에서도 66.67점. 조사/어미 문제가 아니라 실제 답변이 평택시 BIS와
  인천공항 ERP의 80:20 정보를 찾지 못한 검색/답변 내용 문제임.
- c20(큼/큽니다): v2와 v3 모두 100점.
- g16(10%): 현재 생성 답변이 이미 10%라고 답해 v2와 v3 모두 100점.
  다만 v3는 향후 원문대로 "100분의 10"이라고 답해도 통과함.
- g11: 0점. 모델이 참가자격 근거를 찾지 못하고 기권했으므로 채점기 문제가 아님.
- g21: v3.0.1 0점. 답변이 실제 참여 가능 여부를 말하지 않고 원문 확인이
  필요하다고 기권했으므로 형태소가 일부 겹쳐도 인정하지 않음.
- g22/g24: v3에서 올바른 완전 기권으로 각각 100점.

7. 실행 파일

- src/evaluation/scoring_v3/scorer.py
- src/evaluation/scoring_v3/_base.py
- notebooks/20260909_rescore_v3_kongseok.ipynb
- tests/test_scorer_v3.py

저장소를 최신화한 뒤 노트북을 `myenv` 커널에서 실행한다.
기본 입력 v2 실행 ID는 20260909T075822Z로 설정되어 있다. 생성 API를 호출하지
않으므로 API 비용 없이 빠르게 재채점한다.

결과는 다음 위치에 새 실행 시각 폴더로 저장된다.

/home/kongseok/sprint-public-procurement-rag-assistant/output/scoring_v3_runs/<RUN_ID>

8. 한계

- v3도 의미 기반 LLM Judge가 아니라 결정론적 어휘 보조 채점기다.
- 형태소가 같지 않은 광범위한 동의어와 문장 의미는 완전히 판단하지 못한다.
- Golden Set v3의 79문항은 모두 review.status=draft이며 사람 승인 전 상태다.
- 점수 상승이 곧 모델 성능 상승을 뜻하지 않는다. 동일 모델 답변을 더 적절한
  표기 규칙으로 다시 채점한 결과다.
