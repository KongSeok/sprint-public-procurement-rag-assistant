# EVO35.3d+ solo relay - non-golden policy learning

Run: `evo35-3d-20260909`. Date: 2026-09-09. Mode: **solo relay-shot**. Base: `origin/feat/hotline-runtime@e4be5b7`. Worktree: `evo35-training-data`. Branch: `feat/evo35-training-data`.

## 0. Why QLoRA is not the EH core

EvoHarness (EH) is the policy/runtime architecture: given bounded Belief/Progress/Experience state and the currently legal action schema, the Qwen3.5 policy emits the next strict action JSON, observes the tool result, then chooses again until termination. The learned object is therefore a multi-step next-action policy, not a standalone branch classifier.

QLoRA is only the parameter-efficient optimization mechanism proposed for SFT. It does not define EH and can be replaced by LoRA or another compatible adapter method without changing the action/state/tool contract.

"Branch learning" is useful shorthand only if it includes both choosing the next legal tool (`search`, `read`, `track`, `commit`, `recall`, `note`, `finish`, plus enabled visual tools) and generating that action's structured arguments. The policy is invoked again after every observation, so it learns multi-step trajectories rather than one static route choice.

## 1. Frozen safety boundary

- Mini131 remains an exposed historical benchmark, never TRAIN/DEV/sealed-holdout curriculum.
- Do not derive training questions, paraphrases, rewards, prompts or hyperparameters from individual Mini131 failures.
- Preserve the accepted runtime repair already integrated into `feat/hotline-runtime`.
- Preserve typed evidence handles `hist:*`, `cand:*`, `ev:*`, exact action JSON and existing tool/scope/budget guards.
- Raw questions, trajectories and holdout contents stay private; public receipts are hashes/counts/aggregate metrics only.
- Real SFT/GRPO runs only in an isolated compatible training environment with frozen resource limits.

## 2. Relay target

### EVO35.3d - non-golden collection and split freeze
1. Inventory evaluation/Mini131 exclusion identities and candidate corpus/group/doc-pair inventory.
2. Build a hash-only exclusion manifest covering the historical evaluation surface.
3. Create new task groups across fact / compare / follow-up / abstain / visual / mixed without using Mini131 failures as templates.
4. Freeze `sealed_holdout` first and record its immutable hash before tuning.
5. Freeze TRAIN and DEV with zero unresolved question/group/conversation/doc-pair collisions.
6. Collect private Qwen3.5 policy trajectories on TRAIN/DEV only.
7. Export only completed valid next-action JSON targets for SFT; no hidden CoT/gold/qrels/reference/judge fields.

### EVO35.3e - SFT
Freeze HF base revision, tokenizer/chat-template identity, dataset hashes and isolated dependency receipt. Preflight QLoRA/LoRA resource plan; rank16/alpha32 remains a proposal until measured. Train the next-action policy adapter and select only on non-golden DEV.

### EVO35.4 - optional GRPO
Run only if SFT has reliable tool use. Start from accepted SFT. Reward is externally success-gated; efficiency bonus is allowed only on successful episodes. Do not use Mini131 for selection.

### EVO35.5 - final freeze/evaluation
Freeze final adapter/config/Experience, then run Mini131 POST as historical comparison and run the new sealed holdout once. Compare Fixed Hotline / Evo Base / Evo SFT / Evo SFT+GRPO if retained.

## 3. Immediate batch A - inventory/exclusion
Goal: produce a reproducible content-safe inventory before authoring any new training question. Account for Mini131 129 RAG identities, exclude parser-only entries from policy-training fingerprints, verify source hashes/counts, and emit only hash/count receipts.

## 4. Relay ledger
- Documentation: COMPLETED_START_FORM
- Worktree isolation: COMPLETED (`e4be5b7` base, dirty hotline untouched)
- 3d.A inventory/exclusion: IN_PROGRESS
- 3d.B task authoring/split freeze: PENDING_A
- 3d.C trajectory collection: PENDING_B
- 3e SFT: PENDING_3D_RESOURCE_GATE
- 4 GRPO: OPTIONAL_PENDING_SFT
- 5 final POST + sealed holdout: PENDING_FINAL_FREEZE

Relay rule: continue sequentially until a concrete data/resource/safety blocker. Do not substitute Mini131 inspection for missing non-golden data.

## 5. 3d.A actual receipt

Model-free historical exclusion inventory completed against the current source repo. Mini131 question-bearing RAG sources account for 129/129 rows. Added canonical historical evaluation sources (`evaluation/private/dev.jsonl`, `evaluation/private/eda-gold-10.draft.jsonl`, `golden-set-final/source/golden_testset__v6_.jsonl`) only for leakage exclusion, not curriculum selection.

Hash-only exclusion result: 233 unique normalized question fingerprints, 50 group fingerprints, 11 conversation fingerprints and 34 document-pair fingerprints. Private manifest SHA256: `dac6eb9243a6342c57ec343fa2096f6504ab8c7d50b10d9552bab97efa4ed517`; inventory receipt SHA256: `f32192be7eb43845ac003a62d1a24c883cd3ef655ccef74d9fe8b7f80a9c3191`.

Current corpus verification: 98 documents, manifest SHA256 `6c91d30a4c01b12f1aae8924c88a2e5055446c841f5eabfbf687546fdc1fe1cb`; 4,753 possible unordered document pairs, 16 collide with historical evaluation pair fingerprints, leaving 4,737 structurally available pairs. Historical evaluation references 69/98 current corpus documents; 29 HWP documents are never referenced by the selected historical evaluation sources and are the preferred text-data authoring pool.

3d.A status: **COMPLETED_MODEL_FREE**. No model inference, holdout execution or training occurred. Next relay unit: 3d.B document-group partition and non-golden task authoring, using the 29 never-referenced documents first and preserving split isolation.

## 6. 3d.B actual non-golden seed freeze

Deterministic authoring used only the 29 current-corpus documents not referenced by the selected historical evaluation sources. Documents were partitioned before task generation and are disjoint across splits: sealed_holdout 6, dev 6, train 17. The sealed partition was written first and chmod 0400 before train/dev files were emitted.

Authored 91 private tasks across all six planned families: train 53, dev 19, sealed_holdout 19. Train has fact17 / compare8 / follow_up8 / abstain8 / visual6 / mixed6; dev and sealed each have fact6 / compare3 / follow_up3 / abstain3 / visual2 / mixed2. Evaluator targets are stored in separate private files and are not present in training case JSON.

Frozen bundle SHA256: `a904a13acb620e2d86db0a65a43ff387823e467e4d2bb956f13029a06138d9fb`. Sealed holdout seal SHA256: `b820b42b355dcf3798f7a77936aa391c4c9157875c2e56a23370e7e6a6874cfb`. Sealed cases SHA256: `d4d960951bbec8a007dc40e77fed02b28f422f8fbaf7e3878d26398e2dee446a`; sealed targets SHA256: `4b3a4b6f1ef547e7a92068898b7d41eb4cde0146ab08ad095a3dbbfa11add70c`.

3d.B status: **COMPLETED_SEALED_BEFORE_TUNING**. No sealed holdout execution occurred. Next relay unit: 3d.C collect live Qwen3.5 trajectories on TRAIN/DEV only; visual/mixed collection additionally requires a visual OCR index covering their selected fresh documents.
