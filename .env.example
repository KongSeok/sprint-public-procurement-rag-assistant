# 실제 값은 .env에 저장하고 커밋하지 않는다 (README 11장 참고)

# gpt-5-mini 답변 생성(src/generation), text-embedding-3-small 비교 실험(src/retrieval/embeddings)에 필요
OPENAI_API_KEY=

# (선택) 스캔 PDF OCR 시 tesseract 실행 파일 경로를 직접 지정해야 하는 경우에만 설정
# TESSERACT_CMD=
